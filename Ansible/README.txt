1. sudo apt update
   sudo apt install ansible

2. ansible --version

3. Create Inventory file in which it should include host IP Address

4. With help of ansible command your playbook wwill start running & it install all you applications in host OS

5. Run this command "ansible-playbook ansible-playbook.yaml -i inventory/hosts"
